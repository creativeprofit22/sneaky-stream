// Stream module - WebSocket screenshot streaming
// See README.md for standalone usage

export { StreamServer, type StreamOptions } from './server';
export { ScreenshotCapture, type CaptureOptions } from './capture';
export { injectOverlay, clearOverlay } from './overlay';
